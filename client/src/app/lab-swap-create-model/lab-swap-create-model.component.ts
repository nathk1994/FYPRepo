import {Component, OnInit} from '@angular/core';

@Component({
  selector: 'app-dialog',
  templateUrl: './lab-swap-create-model.component.html',
})

export class DialogComponent implements OnInit{

    constructor() {}
  
    ngOnInit(): void { 
    }
}